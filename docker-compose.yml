version: "3.9"
services:

  tools:
    build: .
    image: mertcingoz/udmi-cli
    platform: linux/amd64
    stdin_open: true
    tty: true
    working_dir: /app
    volumes:
      - type: bind
        source: udmi
        target: /app
      - type: bind
        source: ~/.config
        target: /root/.config
      - type: bind
        source: scripts
        target: /scripts
    environment:
      GOOGLE_CLOUD_PROJECT: udmi-mango-test
      GOOGLE_CLOUD_REGION: us-central1
      GOOGLE_CLOUD_REGISTRY: ZZ-TRI-FECTA
      UDMI_TAG: master
      UDMI_VERSION: 1e70358 #April 12, 2023
      UDMI_ALT_REGISTRY: ZZ-REDIRECT-NA
